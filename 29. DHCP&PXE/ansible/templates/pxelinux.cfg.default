DEFAULT install
LABEL install
  KERNEL linux
  INITRD initrd
  APPEND root=/dev/ram0 ramdisk_size=3000000 ip=dhcp iso-url=http://{{ server_ip }}{{ images_root }}/{{ ubuntu_version }}-live-server-amd64.iso autoinstall ds=nocloud-net;s=http://{{ server_ip }}{{ ks_root }}/
