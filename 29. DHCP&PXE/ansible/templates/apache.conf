<VirtualHost {{ server_ip }}:80>
    DocumentRoot /
    <Directory {{ images_root }}>
        Options Indexes MultiViews
        AllowOverride All
        Require all granted
    </Directory>
    <Directory {{ ks_root }}>
        Options Indexes MultiViews
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
